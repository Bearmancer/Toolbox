# P2.2 - CLI contract truthfulness

Work only in `C:\Users\Lance\Dev\Toolbox-worktrees\sacd-completion-v2`.

1. Correct `sacd-convert` format description to 16-bit only.
2. Correct `dsd-convert` input description to DFF only, or add DSF parsing — decide and record.
3. Confirm rejection message names supported value.
4. Confirm `--keep-iso` help states destructive default clearly.

Acceptance: `--help` for both commands matches actual behavior on every option.

Reporting: per subtask command/diff/raw output/PASS/FAIL/BLOCKED. BLOCKED quotes signature and owner. Write report to sibling `task-14-report.md`.
